import 'package:flutter/material.dart';

class ResisterCompanyScreen extends StatelessWidget {
  const ResisterCompanyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('register company screen'),
      ),
    );
  }
}
