import 'package:flutter/material.dart';

class HomePageCompany extends StatelessWidget {
  const HomePageCompany({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('homepage of company'),
      ),
    );
  }
}
